# TodoListThis application is called todo, where this application contains media that is used as a place for us to write activities or agendas 
that we will do so as not to forget to do. In its development, this application uses js.node with two modules expo API, namely function and props. 
To install this application can be done by downloading the available expo link, then the application is installed on the mobile after it is finished, 
it can be used immediately. 
